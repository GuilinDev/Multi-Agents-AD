/**
 * Solito navigation configuration.
 * Route mapping shared between Next.js pages and Expo screens.
 */
export const routes = {
  home: '/',
  chat: '/chat',
  dashboard: '/dashboard',
  settings: '/settings',
} as const;
